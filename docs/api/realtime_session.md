# Realtime sessions

The realtime helper exposes `connect(...)` methods that return websocket session objects. Those sessions encapsulate the underlying transport, keep-alives, and token events. Use the sync or async session depending on your workflow:

::: soniox.realtime.RealtimeSTTSession

::: soniox.realtime.AsyncRealtimeSTTSession
